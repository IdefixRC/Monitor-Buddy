# PlatformIO
.pio
.cache
.dummy

# VS Code / IDE (PlatformIO-generated)
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch
compile_commands.json

# Claude Code — ignore local/personal settings only; keep shared .claude/ files tracked
.claude/settings.local.json

# Local config — holds the Finnhub API token. Never commit; see config/config.h.example
config/config.h

# Local working notes/scratch — never goes to GitHub
.project/
